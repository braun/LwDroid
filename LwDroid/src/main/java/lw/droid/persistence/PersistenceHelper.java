package lw.droid.persistence;

import android.content.Context;

/**
 * Common task for persistence system:initialization etc. Implementators ususaly generated
 * @author Standa
 *
 */
public interface PersistenceHelper {

	/**
	 * Initialize persistence system (call init on Persistable implementations)
	 * @param ctx application context
	 */
//	void Init(Context ctx);
}
